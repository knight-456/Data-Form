{
  "name": "beyond-the-limits-monorepo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "prettier": "^3.0.3",
    "syncpack": "^15.3.2",
    "turbo": "^1.10.14"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
